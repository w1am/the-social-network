query Profile($username: String!) {
  profile(username: $username) {
    id
    username
    createdAt
    updatedAt
    avatar
    banner
    intro
    friends {
      id
      friendId
      username
    }
  }
}
