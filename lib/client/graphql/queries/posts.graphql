query posts($limit: Float!, $cursor: Float!) {
  posts(limit: $limit, cursor: $cursor) {
    id
    description
    userId
    commentators
    comments {
      id
      comment
      username
    }
    user {
      id
      username
    }
    status
    likes
  }
}