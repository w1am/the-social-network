query posts($limit: Int!, $cursor: Int, $userId: Int) {
  posts(limit: $limit, cursor: $cursor, userId: $userId) {
    id
    description
    userId
    commentators
    status
    likes
    comments {
      id
      comment
      username
    }
    user {
      id
      username
    }
  }
}